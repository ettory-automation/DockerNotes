#infraestrutura/devops/containers/container/docker #infraestrutura/devops/containers/container #infraestrutura/devops

O modo default para o acesso de um container se dá através do usuário _root_, como visto abaixo:

```bash
docker run -it nodeapp:0.1 sh
```

Porém, este tipo de abordagem é arriscada para ambientes de produção, levando em conta a segurança do ambiente. Pensando nisso, foi criada uma forma de atribuir usuários sem privilégios administrativos dentro do container, da seguinte forma:

```Dockerfile
#Imagem base do NodeJS
FROM node:alpine3.21

#Identificador opcional
LABEL maintainer="ettory-martins"

#Adicionando um usuário
RUN adduser -h /var/nodeapp \
	-s /bin/bash \
	-D nodeapp

#Neste caso, alterar o ownership do diretório home do usuário `node` não será necessário. Pois o usuário já será criado com o ownership de seu home correto, utilizando o `adduser` do pacote `busybox`

#Instalando curl
RUN apk add curl

#Mudança de diretório pwd
WORKDIR /var/nodeapp

#Cópia de arquivo da aplicação para ".", que nesse caso é o WORKDIR
COPY app.js .

#Alterando permissões do arquivo da aplicação
RUN chown nodeapp:nodeapp app.js

#Utilização da cláusula ARG
ARG VERSION

#Definição de valor default (caso não seja informado no build)
ENV VERSION=${VERSION:-1.0.0}

#Realizando troca de usuário
USER nodeapp

#Executa a aplicação
CMD [ "node", "app.js" ]
```

